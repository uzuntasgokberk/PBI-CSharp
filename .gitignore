# -----------------------------------------------------------------------------
# Repo-wide OS / editor / tooling noise
# Project-specific ignores live in each subproject's own .gitignore
# (e.g. GetMetrics/.gitignore for exported *.tsv/*.csv data).
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
.vs/
*.suo
*.user
.claude/
